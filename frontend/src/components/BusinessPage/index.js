{/* <ul >
                   <li>{business.title}</li>
                   <li>{business.address} {business.city} {business.state} {business.zipcode}</li>
                   <li>{allTags[business.id]}</li>
                   <li>{business.description}</li>
                   <li><a href={business.websiteUrl} target="_blank">Website</a></li>
                   <li>{business.phone}</li>
                   <li>{business.hours}</li>
                   <img className="business-img" alt='of business' src={business.photoUrl} />
                   {sessionUser && sessionUser.id === business.userId && <button onClick={() => handleEdit(business)}>Edit Business</button>}
                   {sessionUser && sessionUser.id === business.userId && <button onClick={() => handleDelete(business)}>Delete Business</button>}
                   {sessionUser &&  <li>Review Button</li>}
                </ul> */}
